#! /bin/sh
#
# Mount eMMC device
#

#case "$1" in
#        start|"")
#                if [ ! -e /dev/mmcblk0 ]
#                then
#					echo "MMC is not recognized!! Exiting" >> /var/log/mount.log
#                fi
#				mkdir /mnt/boot
#                mount /dev/mmcblk0p1 /mnt/boot
#                res=$?
#                if [ $res -eq 0 ]
#                then
#					echo "Succedded to mount boot partition!"  >> /var/log/mount.log
#                else if [ $res -eq 255 ]
#                then
#					echo "MMC error: Boot partition is not mounted!"  >> /var/log/mount.log
#               	fi
#                fi
#				mkdir /mnt/rootfs
#				mount /dev/mmcblk0p2 /mnt/rootfs
#                if [ $res -eq 0 ]
#                then
#					echo "Succedded to mount boot partition!"  >> /var/log/mount.log
#					cp -a /opt /mnt/rootfs
#                else if [ $res -eq 255 ]
#                then
#					echo "MMC error: Rootfs partition is not mounted!"  >> /var/log/mount.log
#               	fi
#                fi				
#        ;;
#        stop)
#                umount /mnt/boot
#				umount /mnt/rootfs
#        ;;
#        *)
#        echo "Usage: S25mount {start|stop}" >&2
#        exit 1
#        ;;
#esac
